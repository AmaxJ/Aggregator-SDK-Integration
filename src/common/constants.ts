import { BigNumber } from 'ethers'

import { FixedNumber } from '../../fixedNumber'

export const ZERO = BigNumber.from(0)
export const MINUS_ONE_BN = BigNumber.from(-1)
export const ZERO_FN = FixedNumber.fromString('0')
export const MINUS_ONE = FixedNumber.fromString('-1')
export const ONE_FN = FixedNumber.fromString('1')
export const TWO_FN = FixedNumber.fromString('2')
